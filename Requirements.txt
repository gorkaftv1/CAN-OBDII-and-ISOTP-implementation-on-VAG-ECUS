# requirements.txt

# CAN bus y transporte ISO-TP (solo necesario en Raspberry Pi con SocketCAN)
python-can==4.4.2
can-isotp==2.0.3

# Bluetooth Low Energy GATT peripheral (solo necesario en Raspberry Pi)
# Prerequisito del sistema: sudo apt install libbluetooth-dev bluez
bless==0.2.4

# Testing
pytest==8.3.5
pytest-cov==6.0.0
